# Pinned for Python 3.7, matching README's documented 動作環境 where a version is
# specified there; everything else is pinned to the latest release that still ships
# a Python 3.7 wheel, resolved against torch==1.13.1 (CPU-only build — see
# CLAUDE.md's "Dev container" section for why no CUDA/GPU build is installed).
--extra-index-url https://download.pytorch.org/whl/cpu

easydict==1.11
geopy==2.4.1
pyproj==3.2.1
fastdtw==0.3.4
pandas==1.3.5
numpy==1.21.6
torch==1.13.1+cpu
torchvision==0.14.1+cpu
scipy==1.7.3
matplotlib==3.5.3
opencv-python==4.6.0.66
imutils==0.5.4
imageio==2.31.2
Pillow==9.5.0
ipython==7.34.0
PyYAML==6.0.1
tqdm==4.66.1

# Only needed for detection_yolox/train.py, detection_yolox/summary.py,
# detection_yolox/utils/utils_map.py, and deep_sort's own train/eval scripts —
# not imported by main.py's pipeline, but kept here so the whole repo (not just
# the inference entrypoint) installs cleanly.
ipdb==0.13.13
motmetrics==1.4.0
torchsummary==1.5.1
pycocotools==2.0.7
